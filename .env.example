# API keys for topic extraction (Discover tab) and YouTube search.
# Copy this file to .env and fill in your keys:
#   cp .env.example .env
#
# The Settings UI can also set these values (stored in ~/.youtube-audio-chunker/settings.json).

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
YOUTUBE_API_KEY=
